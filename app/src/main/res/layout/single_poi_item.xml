<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/single_poi_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:clickable="true"
    android:elevation="3dp"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless"
    app:cardBackgroundColor="?attr/chipsColour"
    app:cardCornerRadius="10dp"
    app:cardElevation="3dp"
    app:contentPadding="5dp"
    app:strokeColor="@color/gray"
    app:strokeWidth="1dp">

    <TextView
        android:id="@+id/single_poi_txt"
        style="@style/TextAppearance.AppCompat.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:ellipsize="end"
        android:fontFamily="@font/aldrich"
        android:foreground="@drawable/round_ripple"
        android:gravity="center_vertical|start"
        android:maxLines="1"
        android:textAllCaps="false"
        android:textColor="?attr/textColour"
        tools:drawableStartCompat="@drawable/ic_earth"
        tools:text="@string/poi_item" />

</com.google.android.material.card.MaterialCardView>